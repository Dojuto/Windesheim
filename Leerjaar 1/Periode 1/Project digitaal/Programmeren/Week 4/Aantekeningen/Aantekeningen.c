
/*#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdbool.h>
#include <math.h>

// zet je functies onder je main maar benoem je functie dan wel boven de mian en sluit af met (;)

double discriminant(double a, double b, double c)
{
    double discriminant = b * b - 4 * a * c;
    if (discriminant < 0)
        return -1;
    else
        return discriminant;


}

bool groterdan(int a)
{
    if (a > 5)
        return true;
    else
        return false;
}

int telop(int a, int b)
{
    //int functie geeft return terug zie a + b dus wel return
    return a + b;
}

void print()
{
    // void functie geeft geen code terug dus geen return
    printf("ik print\n");
}

float tweemaal(float x)
{
    return 2.0 * x;
}

void main()
{
    /*int resultaat = telop(2, 3);
    printf("Voer de functie uit: \n");
    print();
    printf("E kom weer terug naar de main...:\n");
    printf("%d\n", resultaat);

    int waarde = groterdan(4);
    printf("resultaakt groter dan: %d\n", waarde);

    double a = 12;
    double b = 3;
    double c = -1;

    double d = discriminant(a, b, c);

    printf("discriminant is: %.2f\n", d);*/

    
    


